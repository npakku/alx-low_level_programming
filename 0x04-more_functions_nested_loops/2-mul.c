#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two numbers
 * @num1: first input
 * @num2: second input
 * Return: Always 0 (success)
 */

int mul(int num1, int num2)
{
	int result;

	result = (num1 * num2);
	return (result);
}
